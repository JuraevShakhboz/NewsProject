spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/mydatabase
    username: postgres
    password: 1996
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true